# Copy to .env values, or set as real environment variables before running.
# Without a key the app still runs, using the built-in rule-based fallback.
GEMINI_API_KEY=your-key-here
# Optional, defaults to gemini-2.5-flash
GEMINI_MODEL=gemini-2.5-flash
